/*
    Name: Complete Sentence
    Link: https://judge.beecrowd.com/en/problems/view/1766
*/

#include <bits/stdc++.h>
using namespace std;

int main(){
    map<int, int> wa;
    map<
    map<map<map<int, int>, double>, string> order;
    int cases, total, age, weight, pull, i;
    string name;
    double height;

    cin >> cases;
    for(i = 1; i <= cases; i++){
        cout << "CENARIO {" << i << '}' << endl; 

        cin >> total >> pull;

        while(total--){
            cin >> name >> weight >> age >> height;
            order[] = name;
        }

        for(j = names.begin(); j != names.end(); j++){
            cout << j->first.first << ' ' << j->first.second << ' ' << j->second.first << ' ' << j->second.second << endl;
        }
    }


    return 0;
}